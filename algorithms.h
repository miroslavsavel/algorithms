#ifndef ALGORITHMS
#define ALGORITHMS

//extern int *generate_array(int length);
//extern void print_array(int *pole, size_t length);

extern int *insertion_sort(int *pole, size_t lenghtOfArray);

#endif